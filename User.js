/**
Find which option are available for the current room.

@{input} room
@{output} which directions are not null 
 if direction = null = true then  no print
 otherwise
 print direction
 */

module.exports.whereCanIGo = function (argument) {
 return 0
}